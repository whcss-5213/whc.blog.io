<template>
  <div class="gradient-container">
    <h2>CSS渐变骚操作集锦</h2>
    <div
      v-for="(item, index) in gradientList"
      :key="index"
      class="gradient-item"
    >
      <div class="code-box">
        <h3>{{ item.title }}</h3>
        <pre class="code-content"><code>{{ item.code }}</code></pre>
        <button class="copy-btn" @click="copyCode(item.code, index)">
          {{ copySuccessIndex === index ? '复制成功' : '复制代码' }}
        </button>
      </div>
      <div class="demo-box" :class="`demo-${index}`">
        <!-- 文字渐变 -->
        <div v-if="index === 1" class="text-gradient-demo">渐变文字</div>
        <!-- 文字描边渐变 -->
        <div v-else-if="index === 2" class="stroke-gradient-demo">描边文字</div>
        <!-- 饼图 -->
        <div v-else-if="index === 7" class="pie-demo"></div>
        <!-- 渐变遮罩图片 -->
        <div v-else-if="index === 8" class="fade-img-demo">
          <img src="https://picsum.photos/300/200" alt="渐变遮罩图片" />
        </div>
        <!-- 其他纯背景效果无需内部元素 -->
      </div>
    </div>
  </div>
</template>
<script setup>
import { ref, reactive } from 'vue'

const copySuccessIndex = ref(-1)

const gradientList = reactive([
  {
    title: '条纹背景（无重复图片）',
    code: `.stripes {
  background: linear-gradient(45deg,
    #fb3 25%, #58a 0,
    #58a 50%, #fb3 0,
    #fb3 75%, #58a 0
  );
  background-size: 30px 30px;
}`
  },
  {
    title: '文字渐变（常用）',
    code: `.text-gradient {
  background: linear-gradient(90deg, #42b983, #409eff);
  background-clip: text;
  -webkit-background-clip: text;
  color: transparent;
  font-size: 24px;
  font-weight: bold;
}`
  },
  {
    title: '文字描边渐变',
    code: `.stroke-gradient {
  color: transparent;
  -webkit-text-stroke: 1px transparent;
  background-image: linear-gradient(red, blue);
  background-clip: text;
  -webkit-background-clip: text;
  font-size: 24px;
  font-weight: bold;
}`
  },
  {
    title: '网格背景（代码极简）',
    code: `.grid {
  background-color: #fff;
  background-image:
    linear-gradient(rgba(0,0,0,.1) 1px, transparent 0),
    linear-gradient(90deg, rgba(0,0,0,.1) 1px, transparent 0);
  background-size: 20px 20px;
}`
  },
  {
    title: '波浪效果（纯CSS）',
    code: `.wave {
  height: 40px;
  background: radial-gradient(circle at 10px -5px, transparent 12px, #42b983 13px);
  background-size: 20px 20px;
}`
  },
  {
    title: '切角效果（不用图片）',
    code: `.cut-corner {
  width: 100%;
  height: 100%;
  background: linear-gradient(135deg, transparent 10px, #409eff 0) top left;
  background-size: 100% 100%;
}`
  },
  {
    title: '双层边框渐变',
    code: `.gradient-border {
  width: 100%;
  height: 100%;
  border: 4px solid transparent;
  background-clip: padding-box, border-box;
  background-origin: padding-box, border-box;
  background-image:
    linear-gradient(#fff, #fff),
    linear-gradient(45deg, red, blue);
}`
  },
  {
    title: '饼图（纯conic-gradient）',
    code: `.pie {
  width: 100px;
  height: 100px;
  border-radius: 50%;
  background: conic-gradient(
    #42b983 0 25%,
    #409eff 25% 65%,
    #ffcc00 65% 100%
  );
}`
  },
  {
    title: '透明渐变遮罩（图片渐隐）',
    code: `.fade-img {
  -webkit-mask: linear-gradient(white, transparent);
  mask: linear-gradient(white, transparent);
  width: 300px;
  height: 200px;
}
.fade-img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}`
  },
  {
    title: '玻璃拟态渐变',
    code: `.glass {
  background: rgba(255,255,255,.2);
  backdrop-filter: blur(10px);
  background-image:
    linear-gradient(
      to bottom right,
      rgba(255,255,255,0.3),
      rgba(0,0,0,0.05)
    );
}`
  },
  {
    title: '霓虹发光边框',
    code: `.neon {
  border: solid 2px transparent;
  background-clip: padding-box, border-box;
  background-image:
    linear-gradient(#111, #111),
    linear-gradient(45deg, #0ff, #f0f);
  box-shadow: 0 0 10px #0ff;
}`
  },
  {
    title: '折叠角效果',
    code: `.fold {
  background:
    linear-gradient(to left top, transparent 50%, rgba(0,0,0,.1) 0) no-repeat 100% 0 / 1.4rem 1.4rem,
    #58a;
  width: 100%;
  height: 100%;
}`
  }
])

const copyCode = (code, index) => {
  navigator.clipboard.writeText(code).then(() => {
    copySuccessIndex.value = index
    setTimeout(() => {
      copySuccessIndex.value = -1
    }, 1500)
  })
}
</script>
<style scoped>
.gradient-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 20px;
}
.gradient-container h2 {
  text-align: center;
  color: #333;
  margin-bottom: 30px;
  font-size: 28px;
}
.gradient-item {
  display: flex;
  gap: 20px;
  margin-bottom: 30px;
  padding: 20px;
  border: 1px solid #e5e7eb;
  border-radius: 12px;
  background: #f9fafb;
}
.code-box {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.code-box h3 {
  font-size: 18px;
  color: #2c3e50;
  margin: 0;
}
.code-content {
  padding: 15px;
  background: #272822;
  color: #f8f8f2;
  border-radius: 8px;
  overflow-x: auto;
  font-size: 14px;
  line-height: 1.6;
  margin: 0;
  white-space: pre-wrap;
  word-break: break-all;
}
.copy-btn {
  align-self: flex-start;
  padding: 6px 12px;
  background: #409eff;
  color: #fff;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  font-size: 14px;
  transition: background 0.3s;
}
.copy-btn:hover {
  background: #66b1ff;
}
.demo-box {
  flex: 1;
  min-height: 150px;
  border-radius: 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  border: 1px dashed #dcdfe6;
  padding: 10px;
}

/* 各渐变演示样式 */
.demo-0 {
  background: linear-gradient(45deg, #fb3 25%, #58a 0, #58a 50%, #fb3 0, #fb3 75%, #58a 0);
  background-size: 30px 30px;
}
.text-gradient-demo {
  background: linear-gradient(90deg, #42b983, #409eff);
  background-clip: text;
  -webkit-background-clip: text;
  color: transparent;
  font-size: 24px;
  font-weight: bold;
}
.stroke-gradient-demo {
  color: transparent;
  -webkit-text-stroke: 1px currentColor;
  background-image: linear-gradient(red, blue);
  background-clip: text;
  -webkit-background-clip: text;
  font-size: 24px;
  font-weight: bold;
}
.demo-3 {
  background-color: #fff;
  background-image: linear-gradient(rgba(0,0,0,.1) 1px, transparent 0), linear-gradient(90deg, rgba(0,0,0,.1) 1px, transparent 0);
  background-size: 20px 20px;
}
.demo-4 {
  height: 40px;
  width: 100%;
  background: radial-gradient(circle at 10px -5px, transparent 12px, #42b983 13px);
  background-size: 20px 20px;
}
.demo-5 {
  background: linear-gradient(135deg, transparent 10px, #409eff 0) top left;
  background-size: 100% 100%;
}
.demo-6 {
  border: 4px solid transparent;
  background-clip: padding-box, border-box;
  background-origin: padding-box, border-box;
  background-image: linear-gradient(#fff, #fff), linear-gradient(45deg, red, blue);
}
.pie-demo {
  width: 100px;
  height: 100px;
  border-radius: 50%;
  background: conic-gradient(#42b983 0 25%, #409eff 25% 65%, #ffcc00 65% 100%);
}
.fade-img-demo {
  -webkit-mask: linear-gradient(white, transparent);
  mask: linear-gradient(white, transparent);
  width: 300px;
  height: 200px;
}
.fade-img-demo img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}
.demo-9 {
  background: rgba(255,255,255,.2);
  backdrop-filter: blur(10px);
  background-image: linear-gradient(to bottom right, rgba(255,255,255,0.3), rgba(0,0,0,0.05));
}
.demo-10 {
  border: solid 2px transparent;
  background-clip: padding-box, border-box;
  background-image: linear-gradient(#111, #111), linear-gradient(45deg, #0ff, #f0f);
  box-shadow: 0 0 10px #0ff;
}
.demo-11 {
  background: linear-gradient(to left top, transparent 50%, rgba(0,0,0,.1) 0) no-repeat 100% 0 / 1.4rem 1.4rem, #58a;
}
</style>
